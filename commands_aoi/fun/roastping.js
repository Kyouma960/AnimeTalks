const roasts = require('/home/runner/animetalks/values/roasts');
var roastnum=(Math.floor(Math.random() * 10)+1);
module.exports = {
  name: '',
	description: 'ping roasted..',

message.channel.send(roasts.roastsentences[roastnum]);}
}

